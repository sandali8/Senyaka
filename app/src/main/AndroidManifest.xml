<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.senyakapro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SenyakaPro">
        <activity android:name=".find_payment"></activity>
        <activity android:name=".allpayment" />
        <activity android:name=".cash" />
        <activity android:name=".credit" />
        <activity android:name=".FAQ_Activity" />
        <activity android:name=".Add_FAQ" />
        <activity android:name=".gallery" />
        <activity android:name=".AdminPannal" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Addvehical" />
        <activity android:name=".vehicalHandling" />
    </application>

</manifest>